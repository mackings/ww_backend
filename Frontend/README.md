# WoodWork Frontend

Lightweight Next.js frontend for the WoodWork backend.

```bash
cp .env.example .env.local
npm install
npm run dev
```

The default backend is `https://ww-backend.vercel.app`. Override it with:

```env
BACKEND_API_URL=http://localhost:5000
```

See `ARCHITECTURE.md` for folder ownership and data flow.
